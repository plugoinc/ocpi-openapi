get:
  tags:
    - Credentials
  summary: Receiver - Get credentials
  operationId: getCredential
  security:
    - api_key: []
  responses:
    200:
      description: OK
      $ref: ../components/responses/CredentialResponse.yaml
    401:
      description: Unauthorized
      $ref: ../components/responses/Unauthorized.yaml

