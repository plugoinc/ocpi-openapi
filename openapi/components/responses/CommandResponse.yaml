description: Success
content:
  application/json:
    schema:
      allOf:
        - $ref: ./BaseResponse.yaml
        - properties:
            data:
              $ref: ../schemas/CommandResponse.yaml
