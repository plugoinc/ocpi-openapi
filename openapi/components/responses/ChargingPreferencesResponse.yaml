description: Success
content:
  application/json:
    schema:
      allOf:
        - $ref: ./BaseResponse.yaml
        - properties:
            data:
              type: string
              enum:
                - ACCEPTED
                - DEPARTURE_REQUIRED
                - ENERGY_NEED_REQUIRED
                - NOT_POSSIBLE
                - PROFILE_TYPE_NOT_SUPPORTED
