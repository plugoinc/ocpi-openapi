description: SetChargingProfileResponse
content:
  application/json:
    schema:
      allOf:
        - $ref: ./BaseResponse.yaml
        - properties:
            data:
              $ref: ../schemas/ChargingProfileResponse.yaml
