type: object
required:
  - result
  - timeout
properties:
  result:
    type: string
    enum:
      - NOT_SUPPORTED
      - REJECTED
      - UNKNOWN_SESSION
      - ACCEPTED
    example: ACCEPTED
  timeout:
    type: integer
  message:
    $ref: ../schemas/DisplayText.yaml
