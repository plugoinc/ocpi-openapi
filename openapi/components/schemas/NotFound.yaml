description: Not Found
required:
  - status_code
  - timestamp
type: object
properties:
  status_code:
    type: integer
    enum:
      - 404
  status_message:
    type: string
    enum:
      - Not Found
  timestamp:
    type: string
    format: date-time
